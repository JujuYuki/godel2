

def t0() : call t1();

def t1(a) : ifFor (int i = 0 ; i < x; i++) then
              close a
            else 
 	     ifFor (int i = 0 ; i < x; i++) then
              close a
             else close a endif; endif;

def t2(a) : ifFor (int i = 0 ; i < x; i++) then
              close a
            else 
 	     ifFor (int i = 0 ; i < y; i++) then
              close a
             else close a endif; endif;
